// This file is intentionally left empty.
// The giris page uses robots.txt disallow rules (configured in app/robots.ts)
// to prevent search engine indexing of the login page.
// The page.tsx itself is a "use client" component which cannot directly export metadata,
// but since /giris is already disallowed in robots.ts, this is handled correctly.
